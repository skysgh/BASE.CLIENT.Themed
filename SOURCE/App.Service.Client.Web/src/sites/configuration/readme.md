## Development Directives ##

Configuration specific to sites, which are built up on top of a theme.

Note:
As compared to Settings - which are persisted mutable - 
Configuration remains immutable until redeployed.
