export type TAppsMainConstantsResources = {
  open: {
    root: string;
    images: {
      root: string;
      logos: string;
      trustedBy: string;
      flags: string;
      backgrounds: string;
    };
    files: {
      root: string;
      markdownDir: string;
      pdfDir: string;
    };
  };
  sensitive: {
    root: string;
    images: {
      root: string;
      users: string;
    };
  };
}
