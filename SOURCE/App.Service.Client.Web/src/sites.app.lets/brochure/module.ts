import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

/**
 * Brochure Applet Module
 * 
 * Bounded domain context for marketing/brochure content.
 * Contains:
 * - Features (what the service does)
 * - Capabilities (service capabilities)
 * - FAQs (frequently asked questions)
 * - Endorsements (testimonials)
 * - TrustedBy (client logos)
 * 
 * Used by:
 * - sites.anon (landing pages, marketing)
 * - sites.app (admin management - future)
 */
@NgModule({
  declarations: [],
  providers: [],
  imports: [CommonModule],
  exports: []
})
export class BrochureAppletModule { }
