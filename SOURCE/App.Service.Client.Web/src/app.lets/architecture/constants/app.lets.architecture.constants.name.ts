/**
 * Central constants for the Architecture applet.
 * Single source of truth for naming and paths.
 */

/**
 * Display name for this applet (used in UI, logs, etc.)
 */
export const NAME = 'Architecture';

/**
 * URL path fragment for this applet.
 * Used for both assets (with slashes) and APIs (converted to dots).
 * 
 * Assets: /assets/app.lets/architecture/deployed/
 * APIs:    /api/app.lets.architecture/
 */
export const PATHFRAGMENT = 'app.lets/architecture';
