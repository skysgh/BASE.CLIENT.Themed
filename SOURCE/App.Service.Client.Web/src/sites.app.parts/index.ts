/**
 * sites.app.parts - Platform Applets
 * 
 * System-level features served under /system/* URL prefix.
 * These support users but aren't the primary reason they came to the app.
 */

// This folder contains platform applets loaded via sites.app/routing.ts
// Each applet module is lazy-loaded directly from the main routing.
