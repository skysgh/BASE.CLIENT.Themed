/**
 * Workflows Module
 * 
 * Workflow engine for conditional branching and state management.
 * Used by surveys, wizards, and approval flows.
 */
import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common';

@NgModule({
  imports: [CommonModule],
  exports: []
})
export class WorkflowsModule { }
