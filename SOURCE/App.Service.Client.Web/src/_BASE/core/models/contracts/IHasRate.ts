/**
 * Recurring (eg: Monthly) Rate (see IHasPrice)
 *
 * Note:
 * See SystemServicePricingPlans
 */
export interface IHasRate {
    /**
     * Recurring (eg: monthly) billing rate.
     */
    rate: number;
}

