/**
 * Contract for models that show a specific image.
 * An example could be a User.
 *
 * Not to be confused with imageFileId
 */


export interface IHasImageName {
    imageName?: string;
}

