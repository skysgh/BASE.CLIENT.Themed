import { ReferenceData } from "../../../core/models/data/reference-item.model";

// A **raw** model for interoperability
// and sometimes backing Edit Views of the entity.
export class PlaceType extends ReferenceData {
    // Playground
    // Primary, etc.
}

