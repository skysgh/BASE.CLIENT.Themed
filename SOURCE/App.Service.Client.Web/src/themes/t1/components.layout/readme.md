## Developmennt Directives ##

The page layout is a relatively complex set of coordinated components.

It's put under `components/` rather than under `features/` as users
can't navigate to it itself.

