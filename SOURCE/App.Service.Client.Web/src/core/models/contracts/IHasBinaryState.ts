

/**
 * Probably should not be used
 * as state often turns out to be more complicated.
 * At least an enum of
 * Undefined, Unknown, Y, N...and often more.
 */
export interface IHasBinaryState {
  state: boolean
}


