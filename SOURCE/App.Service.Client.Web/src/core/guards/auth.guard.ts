/**
 * @deprecated This guard has moved to core.ag/guards/auth.guard.ts
 * 
 * ✅ MIGRATION: Guards are Angular-specific and belong in core.ag
 * 
 * This file is kept for backward compatibility.
 * Update your imports to use:
 *   import { AuthGuard } from '../../core.ag/guards/auth.guard';
 * 
 * This re-export will be removed in a future version.
 */

// Re-export from new location for backward compatibility
export { AuthGuard } from '../../core.ag/guards/auth.guard';
