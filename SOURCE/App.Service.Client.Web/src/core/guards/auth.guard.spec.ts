/**
 * @deprecated Tests have moved to core.ag/guards/auth.guard.spec.ts
 * 
 * ✅ MIGRATION: Guards are Angular-specific and belong in core.ag
 * 
 * This file is kept for reference only.
 * Run tests from core.ag/guards/auth.guard.spec.ts
 */

describe('AuthGuard (DEPRECATED - see core.ag/guards)', () => {
  it('tests have moved to core.ag/guards/auth.guard.spec.ts', () => {
    // This is a placeholder to indicate tests have moved
    expect(true).toBe(true);
  });
});
