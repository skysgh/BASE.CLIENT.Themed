import { ReferenceData } from "../../../common/models/reference-item.model";

// A **raw** model for interoperability
// and sometimes backing Edit Views of the entity.
export class PersonGroupRole extends ReferenceData {
  
}

