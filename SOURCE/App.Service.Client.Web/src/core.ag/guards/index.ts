/**
 * Core.Ag Guards Index
 * 
 * ✅ LOCATION: core.ag (Angular-specific)
 * 
 * Guards are Angular-specific (use Router, CanActivate, etc.)
 * so they belong in core.ag, not core.
 * 
 * Re-exports all guards for convenient importing.
 */

export { AuthGuard } from './auth.guard';
export { AccountGuard } from './account.guard';
