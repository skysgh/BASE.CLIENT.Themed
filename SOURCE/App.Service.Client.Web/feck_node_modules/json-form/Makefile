
clean:
	find -name \*~ | xargs rm -f

release: clean
	if [ `npm whoami` != "clbrge" ]; then exit 1; fi
	git branch && read -p "Are you sure ?" R; if [ "$$R" != "y" ]; then exit 1; fi
	GITLAB_TOKEN="zR9xzyVzRdRgH5gj9Lpc" ./node_modules/.bin/release-it


.PHONY: release test
